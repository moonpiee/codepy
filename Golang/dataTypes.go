package main
import "fmt"

func main(){
	//data types are of 4 categories- BARI
	//in go: = -> :=
	//declaration of integers and floats
	a:=15
	b:=68.89

	//declaration of boolean data types
	var boolval bool // by default false
	boolval:=true

	//declaration of strings
	var name stringsstring:="Chans"
	var message string = "Hello, World"

	var cm=message + namefmt.Println(cm)
	//update needed for aggregate and reference data types
}